services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai-chat-app:local
    env_file:
      - .env
    ports:
      - "${SERVER_PORT:-8080}:${SERVER_PORT:-8080}"
    restart: unless-stopped
    init: true
